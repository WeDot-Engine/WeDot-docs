name: 部署

on:
  workflow_dispatch:
  push:
    branches:
      - 'latest'

concurrency:
  group: '部署'
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取本仓库
        uses: actions/checkout@v4

      - name: 取分支列表
        id: get_branches
        run: |
          git fetch --prune
          echo "BRANCHES=$(git branch -r | grep -v '\->' | sed 's|origin/||' | grep -v -E 'class' | tr '\n' ' ')"

      - name: 安装 mdBook
        uses: peaceiris/actions-mdbook@v2
        with:
          mdbook-version: '0.4.37'
          # mdbook-version: 'latest'

      - name: 构建
        env:
          BRANCHES: ${{ env.BRANCHES }}
        run: |
          mkdir -p ./build
          for branch in $BRANCHES; do
            git fetch origin $branch
            git checkout $branch
            echo "切换到分支：$branch"
            mdbook clean && mdbook build
            cp -r ./book ./build/$branch
          done

      # HTML/CSS/JS Minifier
      # - uses: docker://devatherock/minify-js:2.0.0
      #   with:
      #     add_suffix: false     # Optional

      - run: cd ./build
      - name: 生成站点地图
        id: sitemap
        uses: cicirello/generate-sitemap@v1
        with:
          base-url-path: https://docs.wedot.top/
      - run: cd ..

      - name: 部署
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy ./build --project-name=wedot-docs
      
      - uses: actions/upload-artifact@v4
        with:
          name: artifact
          path: |
            ./build/**
